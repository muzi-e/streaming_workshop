[project]
name = "streaming-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kafka-python>=2.3.1",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
]
